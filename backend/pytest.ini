[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: needs a real PostGIS database; skipped automatically when
        DATABASE_URL doesn't point at a reachable one. Run the whole
        suite including these with `docker compose up -d db` first.
